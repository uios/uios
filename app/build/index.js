window.onload = () => {
  
  var dom = {
    doc: document,
    body: document.body,
    main: document.body.querySelector("main")
  };

  dom.doc.onclick = event => on.touch.tap(event);

  /*BLOB*/  
  
};
